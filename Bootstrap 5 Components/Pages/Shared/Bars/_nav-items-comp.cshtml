@* if the current page url matches the url of the navigation item, then add the active class to the navigation item*@

@{
    var currentUrl = @Context.Request.Path;
}

// cretae a list of navigation items
@*var  = new List<NavItem>
    {
    new NavItem { Url = "/Index", Text = "Home" },
    new NavItem { Url = "/Slides", Text = "Slides" },
    new NavItem { Url = "/Cards", Text = "Cards" }
    }         
    
    // loop through the list of navigation items
    // for each navigation item, create a list item
    // display the navigation item text
    // display the navigation item url
    // if the current page url matches the url of the navigation item, then add the active class to the navigation item
    @foreach (var item in navItems)
    {
    <li class="nav-item">
        <a class="nav-link @(currentUrl == item.Url ? "active" : "")" asp-area="" asp-page="@item.Url">@item.Text</a>
    </li>
    }*@
    
